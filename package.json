{
  "name": "rsschool-landing-page",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist --add"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.104.1",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "modern-normalize": "^3.0.1"
  }
}
